// everything from structs can be found in data.
// it didn't make sense to separate the logical class methods
// from the classes.
//
// the tradeoff here is that we are missing 1 module from the
// ISGTD? paper, but I think that the setup now is far more
// (though perhaps not very) idiomatic TS code.
